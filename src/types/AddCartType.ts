/*import { Product } from "@prisma/client";

export interface AddCartType extends Product {
  name: string;
  unit_amount: number | null;
  quantity?: number | 1;
  image: string;
  id: string;
  description: string;
};
*/
